NODE_ENV=production
# NOTE: specialized hosting like cPanel often assigns a port automatically. 
# Only uncomment PORT if running locally or if your host requires it.
# PORT=5000

# Database Credentials
# Create these in cPanel -> MySQL Database Wizard
DB_NAME=mubraizr_logistics
DB_USER=mubraizr_admin
DB_PASSWORD=YOUR_CPANEL_DB_PASSWORD_HERE
DB_HOST=localhost

# Security
# Get these from your Clerk Dashboard (https://dashboard.clerk.com)
CLERK_PUBLISHABLE_KEY=pk_test_PLACEHOLDER
CLERK_SECRET_KEY=sk_test_PLACEHOLDER

# Email Configuration (cPanel SMTP)
# Use your cPanel email account settings
# Find these in cPanel -> Email Accounts -> Configure Email Client
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=info@mubraizresourcesltd.com
SMTP_PASSWORD=YOUR_EMAIL_PASSWORD_HERE
